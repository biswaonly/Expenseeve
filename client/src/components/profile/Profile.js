import React from "react";
import PropTypes from "prop-types";
import "./profile.css";

const Profile = props => {
  return (
    <div className="top">
      <h1>Profile</h1>
    </div>
  );
};

Profile.propTypes = {};

export default Profile;
